{% extends "base.html" %}
{% load url from future %}
{% block main %}

{% if user.is_authenticated %}
{% if user.username == msg.sender.username or user.username == msg.reciepient.username %}
<br>
<div class="pageContent">
	<p> <b> From: </b> {{ msg.sender }} </br> 
	 <b> To: </b> {{ msg.recipient }} </br>
	 <b> Date: </b> {{ msg.date }} </p>
	<div class="smallTitle">{{ msg.subject }}</div>
	<div class="pageContent">
	{{msg.body}}
	</div>
	<br/>
	<div class="smallLink"><a href="/messages/">Back</a></div>
	<br/>
</div>
{% endif %}
{% endif %}
{% endblock %}
